from . import (
    MeshExporter,
    MeshRendererExporter,
    SpriteHelper,
    Texture2DConverter,
)

# WoWS Toolbox does not bundle FMOD or export audio. Keep UnityPy's audio
# converter optional so mesh and texture conversion stay independent.
try:
    from . import AudioClipConverter
except (ImportError, OSError):
    AudioClipConverter = None

__all__ = [
    "MeshExporter",
    "MeshRendererExporter",
    "SpriteHelper",
    "Texture2DConverter",
]
if AudioClipConverter is not None:
    __all__.append("AudioClipConverter")
